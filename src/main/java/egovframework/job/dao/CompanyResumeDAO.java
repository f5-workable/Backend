package egovframework.job.dao;

import org.apache.ibatis.session.SqlSession;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Repository;

import egovframework.job.dto.CompanyResumeDTO;


@Repository
public class CompanyResumeDAO {

	/*
	 * @Autowired private SqlSession sqlSession;
	 */
   
	/*
	 * // 등록 public int addCompanyResume(CompanyResumeDTO companyResumeDTO) { return
	 * sqlSession.insert(
	 * "egovframework.mapper.job.CompanyResumeMapper.createCompanyResume",
	 * companyResumeDTO); }
	 */
   
   // 등록한 기업이력서 dto의 cr_num 조회
   
}