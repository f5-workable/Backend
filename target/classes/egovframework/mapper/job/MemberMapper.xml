<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.mapper.job.MemberMapper">

    <resultMap id="memberResultMap" type="egovframework.job.vo.MemberVO">
        <id property="m_num" column="m_num"/>
        <result property="id" column="id"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="name" column="name"/>
        <result property="phone" column="phone"/>
        <result property="profil" column="profil"/>
        <result property="deleted" column="deleted"/>
    </resultMap>

    <select id="selectMemberList" resultMap="memberResultMap">
        SELECT * FROM member
    </select>

    <select id="selectMemberById" resultMap="memberResultMap">
        SELECT * FROM member WHERE id = #{id}
    </select>

    <insert id="insertMember" parameterType="egovframework.job.vo.MemberVO">
        INSERT INTO member (id, email, password, name, phone, profil, deleted)
        VALUES (#{id}, #{email}, #{password}, #{name}, #{phone}, #{profil}, #{deleted})
    </insert>

    <update id="updateMember" parameterType="egovframework.job.vo.MemberVO">
        UPDATE member SET id = #{id}, email = #{email}, password = #{password},
        name = #{name}, phone = #{phone}, profil = #{profil}, deleted = #{deleted}
        WHERE id = #{id}
    </update>

    <update id="deleteMember" parameterType="java.lang.String">
        UPDATE member SET deleted = true WHERE id = #{id}
    </update>

</mapper>
